import { PlacefiltrPipe } from './placefiltr.pipe';

describe('PlacefiltrPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacefiltrPipe();
    expect(pipe).toBeTruthy();
  });
});
